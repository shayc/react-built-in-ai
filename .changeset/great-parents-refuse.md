---
"@shayc/react-built-in-ai": patch
---

README and docs refresh: outcome-first intro with feature bullets and extra badges (downloads, size, types), corrected Prompt API examples so the first click can authorize the model download (README + `useLanguageModel` JSDoc), fixed the `prompt-api-polyfill` link, added an error/retry branch to the lifecycle UI example, clarified the gate and security wording, lint-safe compaction recipe, absolute GitHub URLs for npm rendering, and a sharper package description.
