---
"@shayc/react-built-in-ai": patch
---

Improve lifecycle and cancellation internals by reliably detaching abort listeners, simplifying model provisioning, and removing redundant lifecycle paths without changing the public API.
