---
"@shayc/react-built-in-ai": patch
---

Docs: clarify the `idle` lifecycle state (it also covers the availability probe and auto-advances to `ready` for an already-downloaded model), fix the Surface section that implied `proofread` streams, and tighten README wording. No code changes — this republishes the npm registry README.
